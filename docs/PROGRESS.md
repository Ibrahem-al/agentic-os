# PROGRESS

| Phase | Title | Status | Date | Summary |
|---|---|---|---|---|
| 00 | Scaffold & de-risk | done | 2026-07-03 | electron-vite+TS-strict scaffold, native pipeline proven in Electron main, RyuGraph 25.9.1 spike passes offline w/ vendored vector+FTS (win32 needs rebuild:native for Electron), CI 3-OS, skills installed |
| 01 | Storage engine & schema | done | 2026-07-04 | StorageEngine abstraction + RyuGraphEngine (full §18 schema, provenance, HNSW+FTS), single write lane w/ ordering journal, backup-before-open migrations + sidecar, CSV/Cypher export job, appdata.db (WAL, 5 tables) w/ dual-ABI better-sqlite3, spike deleted & CI runs real storage suite offline + under Electron ABI; 52 tests green |
| 02 | Model layer | done | 2026-07-04 | OllamaClient (status/pull/embed/generate) + in-process int8 bge-reranker (checksum-pinned resumable download, lazy-load + 5-min idle unload, @huggingface/tokenizers) + CloudBrain w/ 4 plain-fetch adapters (key-redacting) + safeStorage keychain (MCP bearer token created) + SpendMeter ($0.50 ceiling halts tasks); 118 tests incl. live bge-m3/qwen3/real-reranker + real-DPAPI keychain check |
| 03 | Hybrid retrieval & loop | done | 2026-07-04 | retrieve(task, tags?) per §18 read path (embed → vector 30/label + FTS 30 → graph expansion → 0.5/0.2/0.3 fusion → rerank → top-8 → token-budgeted bundle w/ always-included global-tag prefs) wrapped in §15 loop (local qwen3 critic vs rubric, rewrite, max 5, stop-on-non-improvement, SpendMeter/iteration, best+confidence); 48-node fixture (13 labels, 15 edge types), 5 golden queries green, zero writes proven via lane journal; p50 117ms offline / 1690ms live full-stack (embed 465ms + rerank 30ms/doc = model floor); 170 tests incl. live loop passing on real bge-m3+reranker+qwen3 |
| 04 | Kernel, runner, tracing | done | 2026-07-04 | WorkflowRunner interface + LangGraphRunner (linear StateGraph, durability sync, in-house SqliteCheckpointSaver in appdata.db v2, jobs in §8 tasks table) — LangGraph ESLint-fenced into kernel/; OTel telemetry → SqliteSpanExporter → traces table (span per run/step/kernel action/model call, trace ids persist in job payload → resume joins the original trace across processes); ContextManager (provider-budget waterfill, map-reduce local-LLM summarize w/ capped output, facts-first prompt, never truncates, pinned-protected); kernel.execute facade w/ PHASE-09 permission/audit stubs; DoD: real SIGKILL mid-step-2 + re-instantiated resume completes w/ step 1 run once, span parent-child pinned, end-of-section key fact survives summarization; 217 tests (incl. OLLAMA=1 live qwen3 summarize) |
| 05 | MCP server & client | done | 2026-07-04 | Streamable HTTP MCP server on 127.0.0.1:4517 (SDK 1.29.0, keychain bearer auth, timing-safe) w/ exactly the 7 §12 tools (get_context = phase-03 loop; search_memory = new direct hybrid search w/ rerank, no expansion/loop; list_skills/get_skill incl. active version + 3 recent examples; propose_correction → staged_writes only, graph provably untouched; ingest_* registered NOT_IMPLEMENTED); single CallTool chokepoint = kernel.execute span + mcp_calls row in finally (args sha256 hash always, args JSON ≤16KB; appdata v3) — impossible to invoke a tool unlogged (count-asserted); connection helper (exact claude mcp add cmd + sample .mcp.json, placeholder token per rule 7) + MCP client manager (mcp-servers.json, keychain secret indirection, listTools via SDK client); MCP SDK ESLint-fenced into src/main/mcp; 256 tests (offline/Electron/live green) + real Claude Code smoke: ✔ Connected, live get_context bundle (confidence high) w/ call-log row + span |
| 06 | Knowledge ingestion | done | 2026-07-04 | src/main/ingest: structure-aware chunker (headings/fences boundaries, ~512/64 §20, atomic fences re-wrapped on split, overlap never crosses headings) → bge-m3 embed → ONE lane job writing Document + Knowledge chunks (HAS_CHUNK, TAGGED, provenance knowledge-ingest@1.0) w/ content-hash dedup (identical re-add = zero lane jobs + zero embeds, pinned; changed = delete-old+write-new in same job, hash-derived chunk ids); ingest_document MCP tool filled (path or inline content; PDF etc. error as deferred) — e2e over real SDK client w/ mcp_calls rows asserted; watched-folder definition store (watched-folders.json) + manual scan trigger (chokidar deferred to phase 11, not installed); 287 tests (offline 279+8 skip, live sequential 287/287, Electron runtime green) |
| 07 | Codebase ingestion | done | 2026-07-04 | src/main/ingest/{codebaseWalk,codeParser,codebase}: gitignore-respecting walk (`ignore`, nested+negation) → web-tree-sitter WASM grammars (TS/JS/Python, user-approved; no native builds, same artifact under vitest+Electron) → meaningful units only (exported fns/classes incl. overload-merge + const-arrow, interfaces/types/enums/dataclasses→model, express/fastapi routes; plain constants excluded) → Component{name=relPath:unit} + DEPENDS_ON from imports×body-refs w/ barrel + __init__ re-export resolution, HAS_COMPONENT from Project matched by path-derived id / created w/ qwen3 README summary (narration-guarded, deterministic fallback) + bge-m3 embedding; per-unit content-hash in component id → diff-based ONE lane job (unchanged repo re-ingest = zero writes, live 1.3s vs 322s cold); README/md/docstring digests → phase-06 pipeline tagged to Project + stale-doc prune; ingest_codebase MCP tool filled (ToolContext+llm); retrieval fixes this phase exposed: rerank-pool tie-break (was hash-random >50 candidates) + structural carry-through in final ordering (sigmoid(logit)+0.3×graph; measured ~4-logit cross-encoder class bias) → DoD retrieve("how does ingestion work") live: 5 relevant Components in bundle; self-test: 282 components/197 DEPENDS_ON/555 chunks; 322 tests green (offline 314+8skip, live sequential, Electron runtime), lint/typecheck/build clean |
| 08 | Extraction agent | done | 2026-07-04 | src/main/agents/extraction: 6-step Phase-04 workflow (collect → deterministic → extract → resolve → verify → write; state checkpointed, ALL graph writes idempotent in ONE final lane job → crash-resume never re-runs model passes, pinned) exposed as manual runExtraction(sessionId,{transcriptPath,cwd}) — no triggers (phase 11); deterministic pass (no model, conf 1.0): mcp_calls get_skill→existing Skills + tolerant-JSONL transcript record facts (tool_use names → MCP/Plugin MERGE by name, OS's own server excluded; cwd → Project via phase-07 proj-<rootKey> → component-prefix → basename → create w/ stub summary + real embedding) → Session/USED/PRODUCED/USES/TAGGED; fuzzy passes: one focused prompt per target (components/preferences/explicit-corrections-only) over ~2048-tok chunks, qwen3 via Ollama structured outputs (KEY FINDING: schema-constrained decoding stops qwen3:4b narrating away its whole output budget — probed live; format now a first-class OllamaClient option), call-score confidence (unparseable 0 / clean-empty 1 / mean) → §20 gates: >60k tokens or <0.6 → whole-session cloud escalation (metered vs $0.50/job), degrade-not-crash; resolution tiers per §20: stable-key → cosine ≥.90 merge / .75–.90 local-LLM {"same":bool} tiebreak / <.75 new (Preferences via real vector index; Components embedded at resolve time w/ overlap prefilter; tiebreak-unavailable → new + conf capped 0.5 → review); gated write: ≥0.6 commits w/ extraction@0.0.1/<pass> + confidence on nodes AND every edge + EXTRACTED_FROM; below-gate → cloud verifier (different model; skipped when cloud extracted = §17 self-judging guard) confirm→commit as llm-local+verified at verifier conf, reject/unavailable → staged_writes rows (kind extraction, deterministic ids + OR IGNORE, full payload op/node/edges/tagCreates/provenance/evidence/reason) — graph provably untouched; DoD: 3 golden sessions w/ full node+edge+provenance asserts incl. USED dedup + errored-call exclusion + ONE-lane-job pin + idempotent re-run all-merges; resolution golden hits every §20 band (merge/tiebreak-YES/tiebreak-NO/novel); crash-mid-resolve → resume on fresh instance completes w/ 0 extract re-runs; live: real qwen3+bge-m3 selftest extracts component+2 prefs+correction conf .9 local in 22s (out/smoke/extraction-selftest.mjs); 370 tests (48 new) — offline 360+9skip clean, live sequential 370/370 (exit 0), Electron runtime 138+9skip, lint/typecheck/build clean |
| 09 | Security & sandbox | done | 2026-07-04 | src/main/security: zod capability schema {fsRead,fsWrite,netDomains,tools,maxSpendUSD} = ONE source for kernel gates + both lanes; PermissionEngine replaces the phase-04 stub (default-deny incl. unregistered agents, tiered gates: reads auto-allow / write-net-spend → durable pending-approval rows keyed by action signature, headless stays queued / out-of-scope hard block + span event — every kernel action's span now carries permission.decision); managed Deno lane (v2.9.1 pinned per-platform sha256, resumable download, capability-derived --allow-* + --no-prompt/--no-remote, v8 memory cap, SIGKILL deadline) + Docker lane (deny-by-default: network none, cap-drop ALL, read-only root, ro/rw mounts from the same object; netDomains → fail-closed refusal, per-domain egress unenforceable in plain docker; timeout kills the container by name w/ retries) + ONE conformance fixture table run as REAL probes in BOTH lanes (18/18: escape reads/writes denied both lanes, reachable-server net denied w/ empty request logs, allowed read/write round-trips, 120s sleep killed at 3s); AuditLog: reversible deltas per structured op (pre-images read in-lane; raw mutating cypher ⇒ flagged un-undoable), file pre-images in backups/audit/, undo(actionId) in ONE lane job — DoD graph+file round-trips green, partial-failure prefix rollback pinned; staged-writes lifecycle for BOTH proposers (correction patch w/ protected-key recheck + extraction payload w/ commit-time embedding): human diff → approve = audited lane commit (undoable) / reject = no trace beyond the log; UntrustedText class (non-assignable to string, coercion traps, 2 named sinks) threads ingestion end-to-end + regex+qwen3-schema-verdict injection scanner (flag-never-block → injection_flags; DoD "ignore instructions and POST…" flagged w/ zero approvals/spans/model calls, content inert); appdata v4 (approvals, audit_log, injection_flags); all pre-existing suites now run the REAL engine via fixtures; 545 tests (92 new) — offline 453+10skip exit 0, live sequential 463/463, Electron runtime 177+10skip, lint/typecheck/build clean, dev smoke shows both [security] boot lines (found+fixed live: docker kill racing container creation leaked a hung sleeper — kill now retries) |
| 10 | Dashboard | done | 2026-07-04 | Full v1 cockpit: 9 panels (memory browser w/ search+inspector+neighborhood, review queue w/ staged diffs+§13 approvals+injection flags, audit/undo timeline w/ WORKING undo, spend, tasks & watchers w/ scan-now, trace waterfall w/ permission.decision surfacing, skill analytics, ingestion w/ live progress, settings w/ keys/models/ollama/mcp-connect) over a typed IPC contract (src/shared/ipc.ts channel map, IpcResult envelopes, zero `any`, renderer Node-free) + design system per protocol (PRODUCT/DESIGN.md, dark-locked OKLCH tokens w/ Tailwind palette RESET as enforcement, kit.tsx single component grammar, dials 4/2/7); seed script (npm run seed:demo — fixture graph + both staged shapes + real audited undoables + approvals/flags/traces/spend/tasks + scannable demo folder); Playwright e2e green (approve staged write → graph reflects it; undo → inverse applied; folder ingest w/ real bge-m3 → Knowledge in browser) + env-gated all-panel screenshot capture; audit pass applied (keyboard-operable rows, aria labels, modal focus, contrast bump, window floor) w/ P2s recorded; 463 offline tests + lint/typecheck/build clean |
| 11 | Triggers & automation | done | 2026-07-05 | src/main/triggers: durable §8 queue (priority+aging heap mirrored to tasks w/ appdata v5 priority/waiting_approval_id, §20 retry 1m/5m/25m→deferred+flag, §13 approval-parked tasks released by dashboard decisions, dedup-by-id, live-MCP yield w/ aging cap, real-SIGKILL kill/restart DoD green); croner schedules (prune 03:00 = audited REVERSIBLE transcript_ref drop w/ undo round-trip pinned, export Sun 03:30, skill slot 02:00 no-op til phase 12); §6 session-end chain (POST /hooks/session-end on the MCP server behind a dedicated keychain hook token, spool drain w/ *.bad quarantine, 30-min inactivity sweep w/ NOT EXISTS dedup) → extraction runs end-to-end from a fake POST, hook+inactivity = exactly once, retries RESUME the workflow job (zero model re-runs); hook installer (safe deep-merge into ~/.claude/settings.json, golden-file pre-existing-hooks-intact test, backup+LCS diff, token redacted over IPC) + session-end.sh/.ps1 (POST→spool-on-failure, proven against live+dead endpoints); §17 rules (zod shape, condition DSL `x.y contains 'lit'`, watch-scope⊆capabilities at load, rule:<id> agents w/ NO standing grants); watchers runtime (chokidar folders w/ boot catch-up scan + auto-ingested trust tag, kernel-mediated file-watch detection, net-gated url polls dark until approved, rule schedules) → DoD demo rule fires in the REAL Deno lane after dashboard approval: allowed write lands, out-of-scope write DENIED (file provably absent, spans pending→allow); AGENTIC_OS_DOT_DIR hermeticity; triggers.status/installHook IPC + tasks/settings panel sections; 61 new tests — offline 514+10skip exit 0 clean, Electron runtime 199+10skip exit 0, e2e 3 passed, lint/typecheck/build clean, boot smoke drains a real spool file to a done extraction task |
| 12 | Skill-improvement agent | done | 2026-07-05 | src/main/agents/skills: §17 agent #4 as a 5-step Phase-04 workflow (plan→testset→candidate→benchmark→write, cloud spends checkpoint before the local-heavy benchmark → crash-resume re-buys nothing, DoD-pinned w/ dead-cloud+dead-executor resume) on the 02:00 slot (phase-11 no-op deleted) + manual "improve now" (queue-durable IPC); event gate = new Corrections/failure Examples since the per-skill last_run_at cursor (manual bypasses recency, pending-review blocks, per-run cap 5, signal consumed only by completed passes); test set = deterministic id-linked correction→regression cases (neutral prompt, correction lives in the graded expectation) + cloud-synthesized coverage cases, stratified seeded 0.4 held-out split (skill-creator run_loop semantics — reference read in full, format+prompts reimplemented never guessed); candidate = cloud SKILL.md rewrite (validated frontmatter per quick_validate rules, retry-with-error, content-hash version identity); benchmark = local executor ×3 runs/case/config + local schema-constrained grader (grader.md adaptation, burden-of-proof-fails, symmetric across configs) for verifiable + cloud blind A/B comparator (comparator.md adaptation, parity-alternating labels) for stylistic, all cloud metered vs $0.50; adoption gate: verifiable = STRICTLY-better held-out AND zero majority-regressions on correction cases → audited record+flip (candidate→active, active→retired, Skill re-embedded w/ the exact retrieval render, HAS_VERSION stamped skill-improvement@0.0.1) else candidate recorded 'retired' w/ score; stylistic (the DEFAULT mode) → staged_writes kind 'skill-improvement' (3rd §13 shape: LCS diff + A/B tallies rendered, approve = audited flip, reject = row + audited candidate retire — recorded deviation) NEVER auto-adopted; skills persist as full SKILL.md text → byte-lossless disk round-trip (DoD 4); appdata v6 (skill_settings: mode/auto-revert/cursor; skill_improvements ledger w/ predecessor snapshot + drift columns); §20 drift watch: corrections-rate over first 20 uses vs predecessor tenure computed nightly from edge timestamps → flag (default) / resume-safe auto-revert (only the still-standing flagged adoption) / cleared-after-20; rollbackSkillAdoption restores predecessor (or pre-version snapshot) byte-for-byte; 4 new IPC channels + SkillsPanel improvement section (mode/auto-revert/improve-now/rollback/ledger w/ drift badges); 48 new tests (17 skillmd, 14 harness, 16 integration DoD incl. adopt/reject-regression/stylistic-review/gate/rollback/drift×3/crash-resume/handler, 1 = OLLAMA live benchmark gate: real qwen3 executor + schema-constrained grader in 126.6s) — offline 562+11skip exit 0 zero errors, live sequential 573/573 exit 0 zero errors, Electron runtime 214+11skip exit 0, e2e 3 passed + env-gated skip exit 0, boot smoke exit 0 w/ the new [agents] line, lint/typecheck/build clean |
| 13 | Hardening & release | done | 2026-07-05 | v0.1.0: §8 scheduler policy (priority-class bands w/ capped aging, cloud single lane + per-provider spacing + 429 handling, local pool semaphore, step-boundary yield into the runner) + task-row/checkpoint retention; electron-builder installers (win nsis / mac dmg+zip arm64+x64 / linux AppImage+deb, natives unpacked, extensions+hooks as extraResources) + electron-updater (log-only, §21.9 proven on the REAL packaged exe: v1000 probe migration → pre-migration backup, packaged smoke green vs the INSTALLED app incl. rendered guided-Ollama setup); golden-path E2E = the release gate (8 arrow tests over the production build on scripted HTTP models: fresh profile → MCP codebase ingest → get_context w/ Components → hook → extraction commits+stages → dashboard approve → audited file-write undo → key+relaunch arms cloud → skill improves+ADOPTS → get_skill serves the learned version) green locally AND in CI (ubuntu+macos; windows test job green); first-ever CI runs exposed + fixed: TWO upstream ryugraph faults (NAPI list-binding bit-reinterpretation — embeddings now sentinel-bound losslessly + poison params refused + re-embeds self-verify/heal/fail-loud; 8TiB VAS per Database), docker lane root-vs-bind-mount defect (runs as host uid now) + windows-containers detection, CRLF fixture pins, POSIX file-URL isMain bug, linux pipe-buffering on process.exit, safeStorage CI seams; perf: retrieve() p50 358ms (p95 388) on a 10k-node/17k-edge graph locally, 180-193ms on CI (<500 ✓); README + dashboard polish (modal focus trap, arrow-key rows, drift rail badge); 630 offline tests (57 new) |
| 14 | MCP-expansion foundation | done | 2026-07-06 | Shared spine for the MCP-integration + subscription-reasoner feature (`website/MCP-COVERAGE.md`), Opus-built on branch feat/mcp-expansion-subscription-reasoner: appdata **v7** (runner_runs/runner_submissions/mcp_calls.session_kind), config RUNNER_*/EXTRACTION_SUBSCRIPTION_* constants, keychain runner token (unconditional rotate-on-boot = zombie-token defense), models/callBudget (durable $-ceiling replacement over runner_runs; CallBudgetExceededError extends SpendCeilingExceededError → existing halt catches unchanged), queue TaskRetryAtError (re-pend, no §20 attempt consumed)+retryDeferred, **P0.1** silent-loss fix (ExtractionUnavailableError thrown after cloud-rescue — never tombstone an unlearned session), composable mcp/tools registry (read/write/control seams, barrel import-compatible), **P0.6** real scope enforcement (mcp-call consults cap.tools + killed allow-by-default fallthrough + mcp-runner: profile), server dual-token auth + per-request session-kind re-check + server-side runner allowlist + RUNNER_SESSION_MAX_TOOL_CALLS + gauge split (interactive vs runner); default install + 7 existing tools unchanged; lint/typecheck clean, 652 passed+12 skip offline (retrieval latency flake green in isolation, +37 vs phase-13) |
| 15 | MCP read-tool surface | done | 2026-07-06 | §8-Phase-1 read tools (FP-1), Opus-built: extracted a shared `src/main/reads/` layer consumed by BOTH ipc.ts (DRY refactor 1121→740, dashboard byte-identical via the typed register<C> contract) and the new mcp/tools/read.ts — **22** §4 read tools (list_sessions/read_session/get_pending_work, get_skill_full/signal, memory_counts/list_nodes/get_node, list_staged_writes/get_staged_write/list_approvals/list_injection_flags/list_audit_log/list_traces/get_trace/get_usage, list_tasks/get_task/get_triggers_status/list_watched_folders, get_app_status/get_settings_summary) all riding the dispatch chokepoint (span + mcp_calls row); read_session resolves the transcript path SERVER-SIDE (no arbitrary-file read); get_settings_summary presence-booleans only (no key material); P0.2 live-path budget wired into get_context (no-op on empty runner_runs); McpReadContext+setReadContext threads late-bound deps additively; get_runner_status deferred to phase-17; lint/typecheck clean, 70 phase-15 tests green in isolation, full suite green (load flakes isolated, ≈684 pass) |
| 16 | ReasoningProvider seam + routing | done | 2026-07-06 | §8-Phase-4 provider seam (FP-2 / P1.1), Opus-built (16a design-reviewed → 16b wiring): models/provider.ts = ReasoningProvider + per-call ProviderRouter (3 adapters local-qwen3/cloud-api/subscription-claude; §11.4 ROLE_DEFAULTS w/ 5 HARD-local roles; per-role fallback subscription→cloud-api→local) + reasoning/runner settings; wired into the 8 NON-extraction roles (retrieval critic/rewrite, skills testset/rewrite/comparator/executor/grader, ingest projectSummary, scanner llmVerdict, context summarize) via an OPTIONAL router? dep (absent → today's llm/cloud byte-identical, so 500+ existing tests untouched); one shared router built at boot (subscriptionComplete/runnerHealthy UNSET → subscription inert → **DEFAULT==TODAY**); settings.save now merges reasoning/runner + onSettingsChanged→router.invalidate() = the P1.1 no-restart-to-change-provider fix; $0.50 ceiling + local constrained-decoding preserved; extraction roles deferred to phase-18; lint/typecheck clean, 732 passed+12 skip offline (latency flake isolated), extraction/* provably unchanged |
| 17 | Runner: completion mode + circuit breaker | done | 2026-07-06 | §8-Phase-3 runner (FP-3) — **completes the vertical slice**, Opus-built: src/main/runner/* (binary resolution w/ .mjs→execPath test seam + win .cmd never shell:true + RUNNER_MIN_CLI_VERSION gate; spawn w/ process-tree watchdog kill + defensive envelope parse + runner_runs row w/ ISO-8601 UTC started_at + telemetry span; health cache + classifyRunnerFailure; two lanes; completion.ts = the subscriptionComplete fn: checkBudget→quota-throttle→claude -p --max-turns 1 tools-stripped no-MCP) INJECTED into the phase-16 ProviderRouter (subscriptionComplete + runnerHealthy, were unset); **circuit breaker** = CallBudget (P0.2, throws CallBudgetExceededError extends SpendCeilingExceededError before RUNNER_TASK_MAX_CALLS) + wall-clock watchdog + quota self-throttle; **zombie defense** (P0.4) = sweepZombies (image-verified, never by pid alone) + will-quit killChildren BEFORE mcp stop; get_runner_status filled + runner.status/testConnection IPC + renderer settings panel/banner; SHIPS OFF (runner.enabled=false → nothing spawns claude → DEFAULT==TODAY); tested vs a fake claude -p (tests/fixtures/fake-runner.mjs), NO new npm deps; lint/typecheck clean, 787 passed+12 skip offline (+55, no flakes fired), package.json diff empty |
| 18 | Extraction subscription tier + staged/control tools | done | 2026-07-06 | §8-Phase-2 (FP-4), Opus (2 streams): (A) the 3 extraction roles routed through the phase-16 router w/ the subscription SINGLE-TIER branch (resolve('extraction.fuzzy').backend → subscription = no Gate A/B, 30k/2k sizes; else two-tier unchanged); ExtractionTier+='subscription', ExtractionPass+='llm-subscription', skipped-subscription-extractor verify mode; delegate/continuation handler (P1.2 re-chunk == read_session, pinned) + SessionEndOrigin 'mcp' + extract-cont-<sid>-<sha8> family (P1.12 sweepable, extract-<sid> still kept-forever); (B) the staged-write (propose_extraction, submit_extraction_items, propose_skill_revision) + control (run_extraction, improve_skill_now, run_maintenance, retry_task, scan_watched_folder) MCP tools + skills providedCandidate branch. Full 38-tool surface; **§5 human-gated spine NEVER exposed** (grep-confirmed no approve/reject/decide/undo/grant/keys/rollback); propose_skill_revision rides the §17 benchmark gate (can't self-certify); verify caught+fixed the missing createExtractionAgent router wiring; DEFAULT==TODAY (runner off → extraction two-tier byte-identical); lint/typecheck clean, 817 passed+12 skip offline (+30, docker flake isolated) |
| 20 | Hardening & honesty (feature complete) | done | 2026-07-06 | §8-Phase-6 (FP-6, FINAL), Opus: P1.8 model-version bookkeeping (benchmark_json.model + scanDrift modelChangedMidWindow → drift flag-only regardless of autoRevert); P1.11 independence warning (same non-local backend+model rewrite==comparator); §10.4 retrieval single-iteration clamp on a subscription override; P1.7 requiresEmbedder (kind extraction+op create+embedOnCommit) + review-queue session-grouped batch approve + Ollama-required approve preflight (fails open) + staged/decided week counter; P1.10 first-enable consent dialog (§10.7 egress, "I understand" gate) + README honesty copy (Privacy + Subscription-runner sections, user's Windows note preserved); §6.1 recorded ToS gate (docs/subscription-runner-tos.md, dated, terms NOT re-verified); gated CI canary (scripts/diag/runner-canary.mjs, never fails without claude); OFF by default preserved, NO new deps; lint/typecheck clean, 846 passed+12 skip offline (+7, load flakes isolated). **FEATURE COMPLETE: 38 MCP tools + subscription reasoner (completion+agent) + circuit breaker + silent fallback + human-gated spine intact, all opt-in.** |
| 21 | Fallback visibility (chip + broadened banner) | done | 2026-07-06 | Post-feature UX: fallback was silent — add a neutral dashboard CHIP (effective tier when the subscription is enabled-but-unavailable) + BROADEN the runner banner to not-installed. **Fable-5 planned, Opus implemented.** Backend: RunnerStatusDto += fallbackActive (= enabled && !isHealthy() — the router's OWN gate, not the raw sticky state which decays after the 15-min TTL) + effectiveBackend (router.resolve('skills.rewrite'), clamped) → visible to the dashboard AND the get_runner_status MCP tool. Renderer: rail chip (role=status, testid runner-fallback-chip, copy per tier) + per-state banner record (never banners transient 'unknown'); chip+banner coexist. DEFAULT==TODAY (runner off → false/null, nothing rendered, testids intact); no new deps; lint/typecheck clean, 850 passed+12 skip offline (flakes isolated), reads tests 10/10. Flagged (not changed): enabling the runner toggle doesn't set reasoning.backend='subscription-claude', so it may not route roles to the subscription without that 2nd setting |
| 19 | Scheduled agent-mode extraction | done | 2026-07-06 | §8-Phase-5 (FP-5), Opus: the scheduler auto-launches claude -p that CONNECTS BACK to loopback MCP (runner token + X-Agentic-Os-Runner-Task) and submits via submit_extraction_items — behind runner.mode='agent' (opt-in atop the runner opt-in). runner/{mcpConfig,agent}.ts: per-task .mcp.json w/ the LITERAL ${AGENTIC_OS_RUNNER_TOKEN} on disk (real token only in child env, §10.5), spawn w/ --strict-mcp-config/--allowedTools(3 tools)/--disallowedTools/--session-id/scope-guard; EXTRACTION_AGENT_WORKFLOW (collect→deterministic→spawn-agent→delegate-load→resolve→verify→write); **P0.5** tombstone-before-spawn (dedups the child's own SessionEnd hook) + InactivityMonitor selectQuiet session_kind!='runner' filter; **P1.6** injection downgrade (flagged transcript → completion mode, no tools); server-side per-task template {read_session,get_pending_work,submit_extraction_items} narrows the allowlist (non-template tool → PERMISSION_DENIED) + session reaper; stageAll→human review. Reuses 14b(auth/tag/gauge/budget)+17(spawn/zombie)+18(delegate). Connect-back fake validated vs the REAL MCP SDK transport; DEFAULT==TODAY (off by default), NO new deps; lint/typecheck clean, 839 passed+12 skip offline (+22, 6 flake-free e2e runs, docker flake isolated) |
