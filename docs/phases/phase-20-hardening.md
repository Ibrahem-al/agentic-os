# Phase 20 — hardening & honesty (§8 Phase 6; P1.7,8,10,11; §10.4; FP-6, final)

The last phase: the remaining P1 items + the honesty/consent copy that the ToS/privacy gate (§6.1/§10.7) requires before the subscription is ever advertised. Three disjoint streams (backend / renderer / docs+CI) → verify.

**Prime directive:** ADD, never REMOVE. Everything still ships OFF by default; a default install is unchanged.

## Stream A — backend hardening (`agents/skills/*`, `models/*`, `security/stagedWrites.ts`)
- **P1.8 model-version bookkeeping** — the runner already records the resolved `runner_runs.model` (17). Stamp that model id into `skill_improvements.benchmark_json` and the drift `details` blob (`agents/skills/state.ts`/`gate.ts`); when the model id differs across a drift window's endpoints, annotate the finding "model changed mid-window" and treat it as **flag-only regardless of `autoRevert`** (`scanDrift`) — a global model bump must never auto-revert a skill.
- **P1.11 independence warning** — at the settings load/router-resolve path, emit a boot/log warning when `skills.rewrite` and `skills.comparator` resolve to the **same backend+model** (§17's different-tier mandate, enforced as a warning, not a hard block — the benchmark's majority + zero-regression rule still stands).
- **§10.4 retrieval single-iteration clamp** — in `ProviderRouter`/the retrieval path, when `retrieval.critic`/`retrieval.rewrite` are HARD-overridden to `subscription-claude`, force `maxIterations: 1` (one critic pass, no rewrite loop) so a live `get_context` can't fan out to ~9 subscription spawns and trip the client MCP timeout. (These roles are HARD-local by default, so this only bites the deliberate override.)
- **P1.7 backend** — `get_staged_write`/the staged-write DTO computes `requiresEmbedder` (kind `extraction` + `op create` + `embedOnCommit`); surfaced so the approve UI can preflight `OllamaClient.status()` (§9.2: under `stageAll`, approving a new Preference needs a live Ollama at click time — the approve must warn, not error).

## Stream B — renderer (review-queue UX + consent) — all renderer files
- **P1.7 review-queue batch UX** — group the review panel by source session with a per-group **approve-all** (after rendering the diffs), plus a "staged this week / decided this week" counter on the dashboard home so `stageAll` backlog growth is visible. **Ollama-required approve preflight:** disable the approve button with "Ollama required to commit this item" when `requiresEmbedder && OllamaClient.status() !== 'ready'`.
- **P1.10 first-enable consent dialog** — when the user first flips the runner toggle on, a dialog stating the §10.7 egress in plain words: *"With the subscription runner enabled, the text being reasoned about — session transcripts, skill feedback, and (if you opt those roles in) retrieved memory snippets — is sent to Anthropic under your Claude account, the same vendor your Claude Code sessions already go to. Your memory graph, embeddings, and search index never leave your machine."* Require explicit acknowledgement before the toggle persists.
- Cheap P2 polish if it fits the existing kit: a per-window quota sparkline + a runner run-history list off `get_usage`/`runner.status` (deferred from 17). Skip if it needs new deps.

## Stream C — docs + CI + ToS gate
- **P1.10 README honesty** — replace the "local-first learning" copy with the two honest sentences (§10.7): memory graph/embeddings/index never leave the machine; with the runner enabled the reasoned-over text goes to Anthropic under the user's Claude account. **Preserve the user's pre-existing working-tree README edit** (the Windows rebuild note) — add alongside, don't clobber.
- **§6.1 recorded ToS re-check** — a dated note in this phase report + a `docs/` note: the runner ships OFF by default; before any default is ever flipped toward the subscription, the current Anthropic usage policy for scheduled headless subscription use must be re-confirmed. This is a recorded gate, not code.
- **CI canary (P2, gated)** — a script `scripts/diag/runner-canary.mjs` that (when `claude` is present) spawns `claude -p --output-format json` with a trivial prompt and asserts the envelope fields (`is_error`/`result`/`session_id`), catching upstream CLI drift; wire it as a **gated/skipped** CI step (never blocks a run without `claude` installed) + document it. Do NOT install `claude` in CI.

## DoD
`npm run lint && npm run typecheck && npm test` green (offline; flakes isolated). Tests: `requiresEmbedder` computed for embed-on-commit extraction creates; the approve preflight disables when Ollama down (renderer covered by typecheck + e2e); drift is flag-only when the model changed mid-window even with `autoRevert=true`; the independence warning fires on same-backend+model rewrite/comparator; the retrieval clamp forces single-iteration on a subscription override. Everything OFF by default; a default install is unchanged. Update the website repo copy note (out of this repo — record it). This closes the feature: 38 MCP tools, the subscription reasoner (completion + agent mode) with the circuit breaker + silent fallback, the human-gated spine intact.
